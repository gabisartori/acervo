" Tab indentation size (Makes \t to appear as n spaces)
" Left to the default value of 8 because for some reason tabs suck so I'm not gonna use them
" set tabstop=4
" Automatic indentation for when breaking lines; 4 spaces is already the default value
set shiftwidth=4
set expandtab
" Syntax highlighting
syntax on
" Show line numbers
set number
" Highlight the current line the cursor is on
set cursorline
" Set clipboard to be the same as the system instead of its own
set clipboard=unnamedplus

